/* Introduces steps to build and test the be-bookatop on dev environment */

//@Library('bookatop-shared-libs')
import com.bookatop.deployment.DeployBuilder

def static branches() {
    /*
    def getTags = ("git ls-remote -t -h git@github.com:bookatop/be-bookatop.git").execute()
    return getTags.text.readLines().collect {
        it.split()[1].replaceAll('refs/heads/', '').replaceAll('refs/tags/', '').replaceAll("\\^\\{\\}", '')
    }
    */
    return ['main']
}

pipeline {
    agent any

    parameters {
        choice(
                name: 'branchName',
                choices: branches(),
                description: 'Choose branch name*'
        )
        validatingString(
                name: 'serviceName',
                defaultValue: 'auth',
                description: 'Put the service name*',
                failedValidationMessage: 'Service name is empty',
                regex: /^.+$/
        )
        validatingString(
                name: 'buildNumber',
                defaultValue: '1',
                description: 'Put the build number*',
                failedValidationMessage: 'Build number is invalid',
                regex: /^[0-9]+$/
        )
    }

    stages {

        stage('Deploy') {

            steps {

                script {

                    servicePlace = "${branchName}:${serviceName}-service:${buildNumber} on $ENVIRONMENT"

                    echo "Deploy $servicePlace"

                    daemonName = "${serviceName}.service"

//                    DeployBuilder.deployBackEndOnDev(
//                                    printStream,
//                                    "${branchName}",
//                                    "${serviceName}",
//                                    "${daemonName}",
//                                    "${buildNumber}".toInteger(),
//                                    "${SOUR_PATH}",
//                                    "${DEST_PATH}")

                    sh libraryResource 'com/bookatop/scripts/deploy-utils.sh'

                    echo "The $servicePlace is success deployed"
                }
            }
        }
    }
}

//        stage('Init') {
//            steps {
//                script {
//                    library "bookatop-shared-libs@main"
//                }
//            }
//        }

//                    if (this instanceof Script){
//                        echo "Executed under Script"
//                    } else {
//                        echo "Exucuted NOT under Script" + this.className()
//                    }
//                    echo "###################"
//                    echo this.getBinding().getVariables().toString()
//                    echo this.getBinding().getVariables().toString()
//                    echo this.getBinding().getVariables().get("steps").getClass().getTypeName()
//
//                    if (this.getBinding().getVariables().get("steps") instanceof GroovyObjectSupport) {
//                        echo "+GroovyObjectSupport"
////                        if (this.getBinding().getVariables().get("steps").getProperty("out") != null) {
////                            echo "getProperty != null"
////                        } else {
////                            echo "getProperty == null"
////                        }
//                    } else {
//                        echo "-GroovyObjectSupport"
//                    }
//                    getContext().get(TaskListener.class).getLogger().println("ddddddddddddd");
//                    getContext hudson.FilePath
//                    echo this.getBinding().getVariables().get("steps").getDeclaredMethods().toString()
//                    library('bookatop-shared-libs').com.bookatop.deployment.DeployBuilder
//                            .deployBackEndOnDev(
//                                    "${branchName}",
//                                    "${serviceName}",
//                                    "${daemonName}",
//                                    "${buildNumber}".toInteger(),
//                                    "${SOUR_PATH}",
//                                    "${DEST_PATH}")
//                    library('bookatop-shared-libs@main').com.bookatop.deployment.DeployBuilder

//getProperty("out").print("Binding Line")
//
//                    echo DeployBuilder.getClass().getTypeName()
//                    echo DeployBuilder.getClass().toString()
//                    echo DeployBuilder.getClass().getDeclaredFields().toString()
//                    echo DeployBuilder.getClass().getDeclaredMethods().toString()
//
//                    echo "--------------"
//
//                    echo this.getClass().getTypeName()
//                    echo this.getClass().toString()
//                    echo this.getClass().getDeclaredFields().toString()
//                    echo this.getClass().getDeclaredMethods().toString()

// System.out.print("Binding Line");

//                    ByteArrayOutputStream buf = new ByteArrayOutputStream()
//                    PrintStream printStream = new PrintStream(buf)
//
//                    DeployBuilder.deployBackEndOnDev(
//                                    printStream,
//                                    "${branchName}",
//                                    "${serviceName}",
//                                    "${daemonName}",
//                                    "${buildNumber}".toInteger(),
//                                    "${SOUR_PATH}",
//                                    "${DEST_PATH}")
//
//                    echo buf.toString()